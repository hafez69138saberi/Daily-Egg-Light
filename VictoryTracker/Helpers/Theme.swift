import SwiftUI

struct Theme {
    static let primaryBackground = Color(hex: "#3D4363")
    static let accent = Color(hex: "#6978B4")
    static let success = Color(hex: "#3EC45C")
    
    static let cardBackground = Color(hex: "#4A5273")
    static let bronze = Color(hex: "#CD7F32")

    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.7)
}


